#include <iostream>

using namespace std;

class Node {
private:
	Node left;
	Node right;
	object data;

public:

	char data;
	Node* LEFT;
	Node* RIGHT;

	Node(object data) {
		data = element;
		LEFT = nullptr;
		RIGHT = nullptr;
	}
};

int main(void) {
	char P, L, R; //부모, 자식 2 노드

	int num_nodes = 0;
	cin >> num_nodes;

	cin >> P >> L >> R;

	Node* Root;

	Node* Root = Node* first;
}